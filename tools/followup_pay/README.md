# Follow up · ตามโอนเงิน (preview 23 ก.ย. 2026)

หน้า "Follow up" ใน admin-hub/preview/ มี 2 รายการ สลับด้วยปุ่มใหญ่ด้านบน
- 💬 ตอบแชทค้าง · Auto reply (รายการเดิม) — เปลี่ยน checkbox 2 อันเป็นตัวเลือกกลุ่มแบบเลือกทีละกลุ่ม
  (ไม่เคยมีแอดมินคุย · เคยคุยแล้ว ถามรอบใหม่ · รวมสองกลุ่ม · ไม่ต้องตาม) และตัวเลขในกล่อง "อ่านทั้งห้องแชท" กดแล้วเปิดรายชื่อได้
- 💸 ตามโอนเงิน · ส่งราคาแล้วยังไม่โอน (ใหม่) — build_pay.py → key "pay" ใน followup.json

ไฟล์ในโฟลเดอร์นี้ = ของที่ต้องย้ายไป root ตอน deploy
- build_pay.py  → วางที่ root (build_fu.py เรียกเองตอนท้าย)
- build_fu.py   → แทน root/build_fu.py (เรียก build_pay + แก้วันสิ้นสุด LINE = ข้อความล่าสุด ไม่ใช่สิ้นเดือน)
- app.html      → แทน root/app.html และ index.html
tools/auto/refresh.py ไม่ต้องแก้ เพราะ build_fu.py เรียก build_pay.py เอง

Deploy: copy 3 ไฟล์ขึ้น root → unpack.py → build_fu.py → build.py (root) → push
หรือ copy preview/index.html + preview/manifest.json + preview/data/* ไป root (ข้อมูลชุด 23 ก.ย. 05:00) แล้ว copy 3 ไฟล์

## 23 ก.ย. รอบ 2: สถานะ "ตามแล้ว" + ตาราง "แชทไปไหน" + ตัวกรอง Follow up ในข้อมูลดิบ
- build_pay.py: จับการตามจากข้อความในแชท (แอดมินส่งเอง หลังลูกค้าเงียบ >= 12 ชม. และหลังส่งราคา >= 12 ชม.
  ภายใน 30 นาทีนับ 1 ครั้ง) · ส่งการ์ดราคาซ้ำแบบนี้ = การตาม ไม่รีเซ็ตอายุ · กลุ่มใหม่ F ตามแล้ว·รอลูกค้าตอบ 7 วัน
  แล้วกลับเป็น A/B/C พร้อม due=1 · paid_fu = โอนหลังจากมีการตาม · payflow / payfu = ตาราง "แชทไปไหน" 1 และ 7 วัน
- build_fu.py: arflow = ตาราง "แชทไปไหน" ของรายการ Auto reply (ตัดข้อมูลย้อนกลับไป 1 / 7 วันแล้วเทียบกับตอนนี้)
- app.html: ปุ่ม "✓ ตามแล้ว (นอกแชท)" เก็บในชีตส่วนกลางเดิม (st=skip, why ขึ้นต้นด้วย "ตามแล้ว") มีผล 7 วัน
  ข้อมูลดิบ FB/IG/LINE มีแถวตัวกรอง "Follow up" + ป้ายสถานะต่อแชท

## 23 ก.ย. รอบ 3: หมวดที่ 3 "คุยแล้วไม่ถึงราคา" + popup เลือกหมวด
- build_pay.py: judge_np / finish_np → key "np", "npflow", "npstat", "np90" ใน followup.json (คอลัมน์เดียวกับ pay)
  เข้ารายการ = ลูกค้าพิมพ์ถามเรื่องคอร์ส + แอดมิน (คน) ตอบข้อความล่าสุดแล้ว + ฝั่งเรายังไม่เคยส่งราคา/สรุปยอดในรอบนี้
  (หลังสลิปผ่านครั้งล่าสุด) · อายุนับจากคำตอบแรกของแอดมิน · กลุ่ม A บอกชั้น+ถามคอร์ส · B ถามทั่วไป · F ตามแล้ว · X ไม่ต้องตาม
  หลุดเมื่อ ส่งราคา (ไปหมวดส่งราคาแล้วยังไม่โอน) · โอน · ลูกค้าถามต่อ (รอแอดมินตอบ) · LINE แท็กสมัครแล้ว
- app.html: เข้า Follow up แล้วเด้ง popup ให้เลือกหมวดก่อน (Auto reply / คุยแล้วไม่ถึงราคา / ส่งราคาแล้วยังไม่โอน)
  แถบบนสุดบอกหมวดที่กำลังดู + ปุ่ม "เปลี่ยนหมวด" · หมวดใหม่ใช้หน้าตาเดียวกับตามโอน (renderPay + puSetMode)
  ปุ่ม ✓ ตามแล้ว / ย้ายไม่ต้องตาม ใช้ชีตส่วนกลางเดิม key "np:<tid>" ไม่ต้องแก้ Apps Script
