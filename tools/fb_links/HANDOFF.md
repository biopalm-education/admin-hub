# ลิงก์ตรงห้องแชท Facebook: ส่งต่องาน (เขียน 22 ก.ย. 2026)

## เป้าหมาย
ทุกห้องแชท Facebook ใน Admin Hub (ข้อมูลดิบทุกเดือน ม.ค.–ก.ย. 2026 · หน้าตามแชท · CSV) มีปุ่ม "เปิดแชทนี้"
ที่เข้าห้องนั้นใน Meta Business Suite ได้ตรง เหมือน 104 ห้องปิดการขาย ก.ค. ที่ทำไว้แล้ว
และห้องใหม่ได้ลิงก์ตามมาเป็นรอบ **รายสัปดาห์** (ผู้ใช้เลือก: เตือน 11:00 → ผู้ใช้เปิดแอป Claude → Claude เก็บ)
ผู้ใช้ **ไม่** ต้องการให้รันอัตโนมัติตอนกลางคืนบนเครื่อง

## รูปแบบลิงก์ (ตรวจแล้ว 22 ก.ย. ห้อง "กิตติประภา ภูมิซองแมว" เปิดถูกห้อง)
`https://business.facebook.com/latest/inbox/messenger?asset_id=691470034208676&business_id=310165158296126&mailbox_id=691470034208676&selected_item_id=<รหัสบัญชีจริงของลูกค้า>&thread_type=FB_MESSAGE`
- รหัสบัญชีจริง = `threadFBID` ใน React props ของแถวรายการแชท (ไม่ใช่ PSID และไม่ใช่ t_<id> จาก Graph API —
  ทดสอบแล้วทั้งสองแบบเปิดห้องล่าสุดแทน) · ลิงก์ `link` ของ Graph API ก็เปิดห้องล่าสุดแทน
- ใน Dashboard เก็บที่ `agg.fbuid` = {รหัสห้องแชท Graph (แถว[19]): รหัสบัญชีจริง} · ปัจจุบันมี 104 ห้อง
- ช่องค้นหาของ Business Suite ใช้หา id ไม่ได้ (ค้นได้แค่เนื้อความ)

## ท่อส่งข้อมูลที่พร้อมแล้ว (ทดสอบผ่าน 22 ก.ย.)
เบราว์เซอร์ในแอป Claude (business.facebook.com) → **form POST** ไป Apps Script v3 (`act:'links'`) → แท็บ "ลิงก์แชท FB"
ในชีต "Admin Hub · แชทที่ย้ายเอง" → workbench อ่านด้วย GET `?t=<token>&act=links` → `{links:[[id, ชื่อ, ts_ms], ...]}` (ค่าล่าสุดต่อ id)
- fetch/XHR จากหน้า Facebook ถูก CSP บล็อก ต้องใช้ form POST (ช่อง `p` = JSON) ผ่าน iframe ซ่อน — ใช้ได้
- URL /exec อยู่ใน `agg.ovurl` (ข้อมูลเข้ารหัส) · token = sha256('bp-ov|' + user:password จาก admin_secret.txt)
- ทดสอบแล้ว 5 ห้องอยู่ในชีต (by = "Claude (ทดสอบรอบแรก)")

## โค้ดเก็บรหัส (วางใน javascript_tool ที่แท็บ /latest/inbox/messenger) — ดู harvest.js
- **ห้ามพึ่ง localStorage** — Facebook ล้างคีย์แปลกปลอมทิ้ง (/ajax/webstorage/process_keys) ข้อมูล 666 ห้องหายไปแบบนี้
  ให้ส่งเข้าชีตทุก ~150–200 ห้องแทน
- ห้ามเขียน fiber walk แบบ recursive (เคยทำเบราว์เซอร์ค้าง) — ใช้ loop จำกัด 25 ชั้น
- แถวใน /latest/inbox/all มี IG ปนมาก ให้ใช้ /latest/inbox/messenger

## ปัญหาที่เจอวันนี้ (22 ก.ย.)
- เลื่อนรายการด้วย `scrollTop +=` ได้ ~670 ห้องแรก (ถึง ~4–6 ก.ย.) แล้ว **รายการหยุดโหลด** (spinner ค้าง)
  ทั้งแบบสคริปต์และแบบเลื่อนเมาส์จริง (computer scroll) — น่าจะโดน Facebook ชะลอหลังโหลดหนัก
- 2 ก.ย. วิธีเดียวกันเคยได้ 8,120 ห้องใน 25 นาที (scrollTop += 550 ทุก 250 ms) — แผนที่ชื่อ→id ชุดนั้นหายไปกับ sandbox
- แผน: ไปช้า ๆ (เลื่อนทีละ ~450px รอ 0.8–1.5 วิ) พักเป็นช่วง ถ้าค้าง >1 นาทีให้หยุดวันนั้น อย่าดันต่อ
  ถ้ารายการเริ่มต้นใหม่จากบนสุดทุกครั้ง ต้องเลื่อนผ่านช่วงที่เก็บแล้ว — ถ้าช้าเกินให้พิจารณาทำเป็นหลายวัน

## ✅ วิธีที่ 2 ที่ใช้ได้จริง: ค้นชื่อทีละห้อง (พบ 22 ก.ย. 16:40 — ทดสอบห้อง Siriporn Vorasart ผ่าน)
ใช้เมื่อรายการเลื่อนไม่โหลด หรือเก็บเฉพาะห้องที่ต้องการ (เช่นหน้าตามแชท / ปิดการขาย)
1. ใน /latest/inbox/messenger พิมพ์ชื่อลูกค้าในช่อง "ค้นหา" (combobox placeholder "ค้นหา") รอ ~3 วิ
2. ผลจะมี 2 ส่วน: "ผู้คน" (อย่าใช้ — id ที่ได้เปิดแล้ว "Messenger User · เกิดข้อผิดพลาด") และ **"การสนทนาใน MESSENGER"**
3. คลิกแถวใน "การสนทนาใน MESSENGER" → URL เปลี่ยนเป็น `selected_item_id=<รหัสบัญชีจริง>` (ห้องนี้ = 1411988193)
   (ในหน้าต่างแคบ คลิกครั้งแรกอาจไม่เปลี่ยน — ตั้ง viewport 1440x900 แล้วคลิกใหม่)
4. ยืนยัน: `document.body.innerText` ต้องมีวลีของลูกค้าจากข้อมูลเรา (ห้องนี้: "แม่ขอลง") แล้วจึงบันทึก
5. ส่งเข้าชีต: POST JSON ตรงจาก workbench ได้เลย `{t, act:'links', by, rows:[[id, ชื่อ, ts]]}` (Content-Type text/plain)
- ใช้เวลาห้องละ ~10 วิ → เหมาะกับห้องเป้าหมายหลักร้อย (หน้าตามแชท ~500 ห้อง ≈ 1.5 ชม.) ไม่เหมาะกับ 17,000 ห้อง
- ห้อง Siriporn Vorasart (t_10242871796556912 → 1411988193) ใส่ใน preview แล้ว (64e3ba9, agg.fbuid = 105 ห้อง) และอยู่ในชีตแล้ว

## จับคู่รหัส → ห้องใน Dashboard (ยังไม่ได้เขียน)
เขียน `link_fb.py` (รันใน workbench หลัง unpack): อ่านลิงก์จากชีต + src/fb_*.json
1. ชื่อห้อง (แถว[0]) ตรงกับชื่อใน Inbox และชื่อนั้นมีห้องเดียวทั้งสองฝั่ง → รับ
2. ชื่อซ้ำ: เลือกห้องที่เวลาข้อความล่าสุดของเรา ≤ ts ใน Inbox และใกล้ที่สุด · ถ้ายังกำกวม → ตรวจแบบเดิม:
   เปิดลิงก์ รอ ~3.5 วิ แล้วหาวลีเด่นของลูกค้า (ข้อความลูกค้าท้าย ๆ ≥12 ตัว ที่ไม่ซ้ำเกิน 2 ห้อง) ใน `document.body.innerText`
   ไม่เจอให้เลื่อนกล่องข้อความขึ้นบนสุด ~9 ครั้งแล้วหาใหม่
3. ลูกค้าเปลี่ยนชื่อได้ และอีกบัญชีอาจใช้ชื่อเก่า — ห้ามรับชื่อตรงอย่างเดียวถ้ามีทางกำกวม
4. เขียนผลลง `agg['fbuid']` (รวมกับของเดิม ห้ามลบ 104 ห้อง) → build_v4/v5/fu ไม่ต้องรันใหม่ → build.py → push
5. เพิ่มขั้น link_fb.py ใน tools/auto/refresh.py (ก่อน build.py) เพื่อให้รอบตี 5 ดึงลิงก์ใหม่จากชีตทุกวันเอง

## จังหวะหลังเก็บย้อนหลังครบ
เตือนทุกวันจันทร์ 11:00 (Asia/Bangkok) → ผู้ใช้เปิดแอป Claude + ล็อกอิน Business Suite → เก็บห้อง 7 วันล่าสุด (~600–700 ห้อง)
→ ส่งเข้าชีต → รอบตี 5 วันถัดไปใส่ลิงก์เอง · ขออนุญาตผู้ใช้ก่อนใช้เบราว์เซอร์บนเครื่องทุกครั้ง

## สถานะอื่นที่เกี่ยวข้อง (22 ก.ย.)
- Live: ef38b2f — ตามแชท FB/IG/LINE, แยกแอดมิน/อัตโนมัติ, ระดับคุณภาพ A/B/C, ย้ายแชทเอง (เลือกแอดมิน 8 คน)
- Apps Script v3 deploy แล้ว (URL เดิม) · ชีตประวัติการย้าย 10 แถว
- sandbox Composio: /tmp เป็น tmpfs 493MB — งานใหญ่ให้ทำใน /root/work (ดิสก์) · รอบตี 5 เมื่อ 22 ก.ย. ล้มเพราะ /tmp เต็ม

---

## รอบ 23 ก.ย. 2026 (11:00) — สิ่งที่เปลี่ยนไปและผลที่ได้

### 🔧 ต้องรู้ก่อนรอบหน้า: โครงสร้างหน้าเปลี่ยน
Facebook เอา `role="row"` ออกจากแถวรายการแชทแล้ว → `hGrab()` เวอร์ชันเดิมได้ **0 ห้อง**
`harvest.js` แก้แล้ว: หากล่องเลื่อนด้วยการไล่หา React fiber ของ thread (กล่องเลื่อนที่เล็กที่สุดที่มีแถวแชท ≥2 แถว)
ไม่พึ่ง role/class อีก — ถ้ารอบหน้าได้ 0 ห้องอีก ให้สงสัยตรงนี้ก่อนเสมอ

### 🔧 ไม่ต้องเอา token เข้าหน้าเว็บอีกแล้ว
เดิมต้องตั้ง `window.OVURL` / `window.OVTOK` แล้ว form POST จากหน้า Facebook (เพราะ CSP บล็อก fetch)
ตอนนี้ใช้วิธีที่ปลอดภัยกว่า: ดึงผลออกจากเบราว์เซอร์เป็น JSON แล้วให้ **workbench** POST เข้า Apps Script เอง
ด้วย `tools/fb_links/send_links.py` (คำนวณ token ในเครื่อง ไม่พิมพ์ออกมา) — ทดสอบผ่าน 23 ก.ย.

### 📉 Facebook จำกัดการโหลดรายการหนักขึ้นมาก
- 2 ก.ย. ได้ 8,120 ห้อง / 25 นาที · 22 ก.ย. ตันที่ ~670 ห้อง · **23 ก.ย. ตันที่ 74 ห้อง**
- reload แล้วลองใหม่ → รอบสองโหลดได้แค่ **8 ห้อง** แล้วตันสนิท (spinner ค้าง, scrollHeight ไม่โต)
- สรุป: **วิธีเลื่อนรายการกำลังตายลงเรื่อย ๆ** อย่าวางแผนพึ่งวิธีนี้เก็บ 17,536 ห้อง
- ✅ แต่ **การเปิดห้องด้วย selected_item_id ยังเร็วและไม่ถูกจำกัด** (ทดสอบ 2 ห้อง โหลดใน ~9 วิ)
  → "วิธีที่ 2 ค้นชื่อทีละห้อง" ยังใช้ได้ และน่าจะเป็นทางหลักต่อไป

### ✅ ผลรอบนี้
- เก็บได้ 74 ห้อง (22 ก.ย. 18:09 – 23 ก.ย. 11:13) ส่งเข้าชีตครบ → ชีตมี 80 ห้อง
- `link_fb.py` (ใหม่) จับคู่ได้ 61 ห้อง · กำกวม 0 · ไม่พบห้องในข้อมูล 19 (ห้องที่เพิ่งทักหลังรอบดึงข้อมูล)
- `agg.fbuid` 104 → **165** (ของเดิมครบทุกห้อง)
- ตรวจยืนยัน 2 ห้องที่เสี่ยงที่สุด (ข้อมูลเราเก่ากว่า inbox 202 และ 192 วัน) ด้วยการเปิดลิงก์จริง — ถูกทั้งคู่

### 📌 วิธีจับคู่ที่ใช้จริง (link_fb.py)
1. ชื่อตรงกัน **และ** ไม่ซ้ำทั้งสองฝั่ง → รับ
2. ชื่อซ้ำ → เลือกห้องที่เวลาข้อความล่าสุดของเรา ≤ ts ใน inbox และห่างไม่เกิน 3 วัน · เหลือผู้สมัครเดียวจึงรับ
3. ยังกำกวม → เขียนลง `tools/fb_links/ambiguous.json` ให้ไปเปิดลิงก์ตรวจเอง
- เวลาใน `src/fb_*.json`: `m[0]` = นาทีนับจากต้นเดือน, ฐานเดือนคิดแบบเดียวกับ `stitch.py::base()` (T0 = 2026-01-01 เวลาไทย)

### ⚠️ ห้ามรัน build.py ทับ preview/
`preview/` มีงาน UI ที่รอ "deploy" อยู่ และ `index.html` ของมันสร้างจาก app.html เวอร์ชันที่ไม่ได้อยู่ใน repo
→ ใช้ `tools/fb_links/apply_preview.py` แทน (เข้ารหัสใหม่เฉพาะ `preview/data/agg.json` ด้วย salt เดิมของ preview)

### ❌ 23 ก.ย. บ่าย — วิธีที่ 2 (ค้นชื่อ) ใช้ไม่ได้แล้วเช่นกัน
ทดสอบห้อง Siriporn Vorasart ห้องเดิมที่เคยผ่านเมื่อ 22 ก.ย.:
- พิมพ์ชื่อในช่องค้นหา → ผลมี **เฉพาะส่วน "ผู้คน"** ไม่มีส่วน "การสนทนาใน MESSENGER" อีกแล้ว
- กด "ค้นหาในการสนทนาใน Messenger" → "ไม่พบผลลัพธ์ที่ตรงกับการค้นหาของคุณ"
  (ตรงกับที่เคยบันทึกไว้ว่า ค้นการสนทนาเป็น full-text บน "เนื้อความ" ไม่ใช่ชื่อ)
- แถวใน "ผู้คน" ไม่มี React fiber ของ thread จึงอ่าน threadFBID ไม่ได้
→ **ตอนนี้ไม่มีวิธีใดใน UI ที่เก็บ id ได้เป็นกอบเป็นกำ** ทั้งเลื่อนรายการและค้นชื่อ

### 🔍 ทางที่ยังไม่ได้ลอง (สำหรับรอบหน้า เรียงตามความน่าจะสำเร็จ)
1. **เรียก GraphQL ของ Business Suite เอง** — ดัก request ของหน้าเว็บด้วยการ hook `window.fetch` +
   `XMLHttpRequest.prototype.send` (โค้ดใช้ได้ ดักได้จริง) แล้วเล่นซ้ำด้วย cursor ของเราเอง
   ติดตรง: hook ติดตั้งหลังหน้าโหลดเสร็จ จึงยังไม่เคยดัก "query รายการแชท" ได้เลย — ดักได้แต่ query ของห้องเดี่ยว
   (BusinessCometInboxThreadDetail*) การกดสลับแท็บ/ตัวกรองก็ไม่ทำให้เกิด query รายการใหม่
   **ข้อสงสัยสำคัญ: รายการแชทอาจมาทาง MQTT/WebSocket ไม่ใช่ HTTP** — ถ้าใช่ ต้อง hook WebSocket แทน
   วิธีที่ควรลอง: ฉีด hook ผ่าน `javascript_tool` **ก่อน** แล้วค่อย `location.reload()` ในคำสั่งเดียวกันไม่ได้
   (hook หายตอน reload) → ต้องหาทาง hook ใน bundle หรือดักที่ระดับ network tool แทน
2. ดูว่า `read_network_requests` ดึง **request body** ได้ไหม (ตอนนี้ดึงได้แต่ response body ตาม requestId)
3. ถ้าทั้งหมดตัน: ยอมรับว่าเก็บได้เฉพาะห้องใหม่รายวันเท่าที่รายการยอมโหลด แล้วสะสมไปเรื่อย ๆ

### 📊 พฤติกรรมการจำกัดที่สังเกตได้ (23 ก.ย.)
- โหลดครั้งแรกหลังเปิดหน้า: ได้ ~74 ห้อง แล้วตัน
- reload: ได้ 8 ห้อง แล้วตันทันที
- ทิ้งไว้ ~20 นาทีแล้วกลับมา: `scrollHeight` โตเป็น 7,050px (≈95 แถว) เอง **แต่แถวเป็นโครงเปล่า**
  เลื่อนผ่านทั้งหมดแล้วอ่าน fiber ได้แค่ 8 ห้อง → ข้อมูลจริงไม่เคยมา มีแต่ที่ว่างของแถว
- สรุป: การจำกัดอยู่ที่ "ข้อมูลของแถว" ไม่ใช่การ scroll — เร่งหรือรอก็ไม่ช่วย

---

## 📣 พี่ปาล์มสั่งงานยังไง (เขียน 23 ก.ย. 2026)

พิมพ์สั่งได้เลยในแชทใหม่ — แต่ละแบบต้องเตรียมของไม่เหมือนกัน

| สั่งว่า | ต้องเตรียม | ใช้เวลา | ได้อะไร |
|---|---|---|---|
| "ลองหาทางเรียก API รายการแชทของ Business Suite เอง" | เปิดแอป Claude + ล็อกอิน Business Suite ค้างไว้ (ไม่ต้องนั่งเฝ้า) | 1–2 ชม. | ถ้าสำเร็จ = เก็บครบ 17,536 ห้องในรอบเดียว · ถ้าไม่ = รู้ผลชัดว่าตัน |
| "ลองเก็บลิงก์ผ่าน Chrome ของผมแทน" | เปิด Chrome ตัวจริง + ส่วนขยาย Claude + ล็อกอิน Business Suite | 30 นาที | ทดสอบว่าการจำกัดผูกกับเบราว์เซอร์ในแอปหรือผูกกับบัญชี |
| "เก็บลิงก์ห้องใหม่" | เปิดแอป Claude + ล็อกอิน Business Suite | 10–20 นาที | ห้องเท่าที่รายการยอมโหลดวันนั้น (ล่าสุด ~74 ห้อง) |
| "อัปลิงก์จากชีตขึ้นแดชบอร์ด" | ไม่ต้องใช้เครื่องเลย | 3 นาที | เอาลิงก์ที่มีในชีตอยู่แล้วไปจับคู่ + ขึ้นหน้าจริง |

**ทุกครั้งต้องพิมพ์ประโยคนี้ด้วย** ไม่งั้นระบบความปลอดภัยจะบล็อกตั้งแต่ต้น:
> "อนุญาตให้ดึง admin_secret.txt และ gh_token.txt จาก Drive มาใช้ในงานนี้ได้"

และถ้าจะให้ขึ้นหน้าจริง ต้องพิมพ์คำว่า **deploy** เสมอ

### ลำดับที่แนะนำ
1. ลอง API ก่อน (คุ้มที่สุด ถ้าได้คือจบ)
2. ถ้าไม่ได้ ลอง Chrome ตัวจริง
3. ถ้ายังไม่ได้ **พักไม่ยุ่งกับกล่องข้อความ 1–2 สัปดาห์** แล้วค่อยลองเลื่อนรายการใหม่
   เหตุผล: 2 ก.ย. ได้ 8,120 ห้อง · 22 ก.ย. 670 · 23 ก.ย. 74 — เป็นเส้นโค้งที่ลดลงตามการใช้งานหนักติดกัน
   น่าสงสัยว่าเป็นโควตาสะสมระดับบัญชี ซึ่งถ้าใช่ การหยุดพักคือทางแก้ ไม่ใช่การเร่ง


---

## ✅ 24 ก.ย. 2026 — ก.ค. ขึ้นหน้าจริงแล้ว 4,050 / 4,067 ห้อง (หน้าจริง + preview/ รอบเดียว)

### วิธีเก็บรายการแบบใหม่ (ใช้แทนการเลื่อนหน้าจอทั้งหมดข้างบน)
- ต้นเหตุที่รอบ 22–23 ก.ย. ได้แค่ 670 / 74 / 8 ห้อง: **การเลื่อนหน้าจอไม่ได้สั่งให้รายการโหลดเพิ่ม** — ไม่ใช่ Facebook จำกัดบัญชี
  รายการแชทมาทาง websocket (`gateway.facebook.com/ws/realtime`) ไม่ใช่ GraphQL/HTTP จึงดัก fetch/XHR ไม่เจอ
- ทางที่ได้ผล: หา component `BizInboxThreadListLayout` (จาก scroller ของรายการ ขึ้นไป ~7 fiber) แล้ว
  **เรียก `props.onRequestMoreRows()` ตรง ๆ แล้วอ่าน `props.threadList`** — ไม่ต้องพึ่ง DOM ห้องที่ไม่ได้ render ก็อ่านได้
  (บางช่วง `canLoadMoreRows=false` ระหว่างรอ sync แล้วจะกลับมาเอง)
- ได้ **10,024 ห้อง (24 ก.ย. → 30 มิ.ย. 22:06) ใน ~35 นาที ที่ 2.5 วิ/ครั้ง** ไม่เจอการจำกัด · โค้ด: `harvest_list.js`
  (BGSTART(เวลาหยุด) → BGSTAT() → BGEXPORT('fb_threads_YYYY-MM.json'))
- ไฟล์ส่งออกผ่านเครื่องผู้ใช้ → Drive (ระบบความปลอดภัยไม่ให้ Claude ขนข้อมูลลูกค้าจาก Business Suite ไป workbench เอง)
  รอบนี้: Drive `fb_threads_2026-07.json` id 11DUUtoGRbFTqhNA1iC6zF5rKbbsmjk5g — rows = [threadFBID, title, timestamp_ms, threadType, threadID]
- **ห้าม commit ไฟล์ fb_threads หรือรายชื่อลูกค้าเข้า repo (repo เป็น public)**

### เกณฑ์จับคู่ (`match_month.py <fb_threads.json> <YYYY-MM> [--write]`)
- เวลาข้อความล่าสุดของห้อง = max ทุกเดือนที่ห้องนั้นอยู่ (นาทีนับจาก 2026-01-01 เวลาไทย = base เดือน + แถว[18][i][0])
- เวลา Inbox → นาที = ts_ms//60000 − epoch_min(2025-12-31T17:00Z) · ชื่อ normalize: NFKC, strip, lower, ยุบช่องว่าง
- **T1**: ชื่อ (ชื่อใดก็ได้ที่ห้องเคยใช้ทุกเดือน) ตรง + เวลา Inbox == เวลาข้อความล่าสุดของเรา (นาทีเดียวกัน) และมีผู้สมัครเดียว
- **T2**: เวลาไม่ตรง แต่ชื่อไม่ซ้ำทั้งสองฝั่ง และเวลา Inbox ใหม่กว่าข้อความล่าสุดในข้อมูลทั้งหมด (ลูกค้าทักต่อหลังรอบดึง)
- ห้ามใช้ Inbox ที่เก่ากว่าข้อมูลเรา · ห้ามใช้ threadFBID ซ้ำ (รวมกับที่มีอยู่ใน fbuid) · ที่เหลือไม่ใส่ลิงก์
- ผล ก.ค.: T1 3,905 · T2 25 · agg.fbuid 165 → 4,096 · ไม่มีรหัสชนกัน
- ห้องเดียวกันใช้ gid เดียวกันทุกเดือน → ลูกค้าเดิมที่ทักหลายเดือนได้ลิงก์เดียวกันทุกเดือน (เดือนอื่นจึงได้ลิงก์เพิ่มตามไปด้วย)

### ลิงก์เก่าที่แก้ (ตรวจด้วยเวลาทุกข้อความ)
- `fbuid['1500917241413994']` เคยเป็น 100073563762876 (ผิด — เป็นของอีกห้อง) → **100000981146871**
- เพิ่ม `fbuid['1802089631153974'] = 100073563762876` (เจ้าของตัวจริงของรหัสนั้น)

### ห้อง ก.ค. ที่ยังไม่มีลิงก์ 17 ห้อง
- ตรงแค่เวลา / ลูกค้าเปลี่ยนชื่อ 5
- ชื่อซ้ำ เวลาไม่ตรง 1
- บัญชี "ผู้ใช้ Facebook" ที่ถูกปิด 8 (ลิงก์ไม่ได้ตลอดไป)
- ไม่อยู่ในกล่องข้อความหลัก 3 (อาจอยู่โฟลเดอร์อื่น เช่น เสร็จสิ้น/สแปม — ยังไม่ได้ลอง)

### สิ่งที่ขึ้นหน้าจริง + preview/ รอบนี้
- `agg.fbuid` 165 → 4,096 และ `agg.fblinkcov` = {เดือน: [ห้องที่มีลิงก์, ห้องทั้งหมด]} — re-seal เฉพาะ data/agg.json
  และ preview/data/agg.json ใต้ salt เดิมของแต่ละ manifest (ไม่ได้รัน build.py)
- `cov.py` นับ fblinkcov ใหม่ทุกรอบตี 5 (ต่อท้าย build_fu.py ใน tools/auto/refresh.py)
- ปุ่มเหมือนกันทุกจุด: หัวแชท / Follow up = "เปิดแชทนี้ใน Inbox ↗" (LINE = "เปิดแชทนี้ใน LINE OA ↗")
- CSV ข้อมูลดิบ FB/IG มีคอลัมน์ "ลิงก์ห้องแชท" · โน้ต FB แสดง "มีลิงก์ตรงแล้ว a จาก b ห้อง ในเดือนนี้/ในช่วงที่เลือก"

### เดือนถัดไป (ยังไม่ deploy)
ไฟล์เดียวกันจับคู่ได้อีก (หลังรวม ก.ค. แล้ว): ส.ค. T1 3,783 · T2 13 → 4,340 / 4,361 · ก.ย. T1 2,367 · T2 23 → 2,741 / 2,749
มิ.ย. และเก่ากว่า ต้องเก็บรายการย้อนต่อ (BGSTART ด้วยเวลาหยุดที่ต้นเดือนนั้น)

### สั่งงานรอบต่อไป
| สั่งว่า | ต้องเตรียม | ได้อะไร |
|---|---|---|
| "deploy ลิงก์ ส.ค." (หรือ ก.ย.) | ไม่ต้องใช้เครื่อง — ใช้ไฟล์ fb_threads_2026-07.json เดิมบน Drive | ขั้นเดียวกับ ก.ค.: match_month.py --write → cov.py → re-seal agg |
| "เก็บรายการย้อนถึง 1 มิ.ย." | เปิดแอป Claude + ล็อกอิน Business Suite · ลากไฟล์ที่ได้ขึ้น Drive เอง | ไฟล์ fb_threads ใหม่สำหรับเดือนเก่า |
ยังต้องพิมพ์อนุญาตเรื่อง admin_secret.txt / gh_token.txt และคำว่า deploy ทุกครั้งเหมือนเดิม


---

## ✅ 24 ก.ย. 2026 (รอบ 2) — ส.ค. + มิ.ย. ขึ้นหน้าจริงแล้ว

- ส.ค. จากไฟล์ `fb_threads_2026-07.json` เดิม: T1 3,783 · T2 13 → **4,340 / 4,361 ห้อง**
  (ลองจับคู่ด้วยไฟล์ มิ.ย. ที่ใหม่กว่าด้วย ได้ชุดเดียวกันทุกห้อง 3,796 ห้อง ไม่มีห้องไหนได้รหัสต่างกัน)
- มิ.ย. จากไฟล์ใหม่ `fb_threads_2026-06.json` (Drive id 1lnYQ4FO_L9RQSOXG4rpl4oB-RMRvTAxV · 12,000 ห้อง ·
  24 ก.ย. 14:28 → 31 พ.ค. 13:30 ต่อเนื่อง ครอบคลุมไฟล์ ก.ค. ทั้งหมด): T1 2,038 · T2 9 → **3,030 / 3,044 ห้อง**
  (รันหลัง ส.ค. — ห้องที่คุยต่อเนื่องหลายเดือนได้ลิงก์จากรอบ ส.ค. ไปแล้ว)
- agg.fbuid 4,096 → 9,939 ไม่มีรหัสชนกัน · re-seal เฉพาะ data/agg.json + preview/data/agg.json ใต้ salt เดิม
- ห้องที่ยังไม่มีลิงก์: ส.ค. 21 (ไม่เจอชื่อในรายการ Inbox/เปลี่ยนชื่อ 19 · บัญชี "ผู้ใช้ Facebook" 2) ·
  มิ.ย. 14 (ไม่เจอชื่อ/เปลี่ยนชื่อ 9 · บัญชีถูกปิด 5)
- เดือนอื่นได้ลิงก์ตามไปด้วย (ลูกค้าเดิมหลายเดือน): ม.ค. 204/803 · ก.พ. 241/941 · มี.ค. 453/2,082 · เม.ย. 499/2,025 ·
  พ.ค. 1,031/4,004 · ก.ย. 880/2,749
- ถัดไป: ก.ย. จับคู่จากไฟล์ มิ.ย. ได้เลย (ไม่ต้องเปิดเครื่อง) · พ.ค. และเก่ากว่า ต้องเก็บรายการย้อนต่อ
  (BGSTART ด้วยเวลาหยุดที่ต้นเดือนนั้น) · ไฟล์ fb_threads ห้าม commit เข้า repo
