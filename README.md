# admin-hub — Admin Performance

โคลนจาก [chat-hub](https://biopalm-education.github.io/chat-hub/) (commit a6241e1, 16 ก.ย. 2026) สำหรับทีมแอดมินโดยเฉพาะ · ใช้รหัสผ่านแยกจากตัวหลัก

Dashboard สรุปแชท Instagram / Facebook ของ BioPalm — เปิดที่
<https://biopalm-education.github.io/admin-hub/> (ต้องใส่ชื่อผู้ใช้และรหัสผ่านของทีม)

## ไฟล์ในรีโปนี้

| ไฟล์ | คืออะไร |
|---|---|
| `index.html` | โค้ดทั้งหมดของ dashboard — **ไม่มีข้อมูลลูกค้าอยู่ในไฟล์นี้เลย** |
| `manifest.json` | salt + จำนวนรอบ PBKDF2 + ตัวตรวจรหัสผ่าน + รายชื่อไฟล์ข้อมูล |
| `data/agg.json` | ตัวเลขสรุปทุกเดือน (โหลดทันทีหลังใส่รหัส) |
| `data/ig-all.json` | บทสนทนา Instagram ทุกเดือน (โหลดตอนกดดู Instagram) |
| `data/fb-YYYY-MM.json` | บทสนทนา Facebook รายเดือน (โหลดตอนกดดูเดือนนั้น) |
| `app.html` | ต้นฉบับของ `index.html` — แก้โค้ด dashboard ที่ไฟล์นี้ |
| `build_v4.py` | คำนวณกรวย 4 ขั้น ความเร็วตอบ และยอดปิดจากสลิป — รันหลัง `unpack.py` |
| `build_v5.py` | แยก “ทักมาแล้วหาย” ใหม่: เฉพาะคนที่กดปุ่มจากโฆษณาอย่างเดียวแล้วเงียบครบหน้าต่าง (14 หรือ 30 วัน) ยืนยันด้วยป้าย Meta “ตอบกลับโฆษณา” — **รันหลัง `build_v4.py` ก่อน `build.py`** |
| `build.py` | เข้ารหัส `src/*.json` เป็น `data/*` + `manifest.json` แล้วคัดลอก `app.html` เป็น `index.html` |
| `deploy.sh` | build แล้ว push ขึ้น GitHub Pages (อ่าน token จาก `gh_token.txt` ที่ไม่ได้อยู่ในรีโป) |
| `extract.py` | สคริปต์ครั้งเดียว ใช้ตอนแตกข้อมูลออกจาก HTML ก้อนเดิม |

## ลำดับการรันเวลาเพิ่มเดือนใหม่

```
python3 unpack.py      # ถอดรหัส data/*.json กลับเป็น src/
python3 build_v4.py    # กรวย 4 ขั้น + ความเร็วตอบ
python3 build_v5.py    # แยก "ทักมาแล้วหาย" แบบเข้ม (14 / 30 วัน)
python3 build.py       # เข้ารหัสใหม่ทั้งหมดลง pages/
```

ข้ามขั้นไหนไม่ได้ และ `build.py` สุ่ม salt ใหม่ทุกครั้ง จึงต้อง push `manifest.json`
พร้อม `data/` ทั้งชุดเสมอ

## นิยาม “ทักมาแล้วหาย” (ตั้งแต่ 20 ก.ย. 2026)

นับเฉพาะแชทที่ลูกค้า **กดปุ่มจากโฆษณาอย่างเดียว** แล้วเงียบ:

- ต้องมีข้อความระบบ **“ตอบกลับโฆษณา”** ที่ Meta ติดมาเอง กำกับการกดปุ่มนั้น (ภายใน 2 นาที)
- ข้อความจากลูกค้าทั้งแชทต้องเป็นปุ่มโฆษณาเท่านั้น — ไม่มีปุ่มเมนู ไม่มีรูป ไม่มีสติกเกอร์ ไม่พิมพ์แม้แต่ “ค่ะ”
- ไม่กลับมาพิมพ์อะไรอีกจนครบหน้าต่างที่เลือก (ไล่ข้ามเดือนจากรหัสห้องแชทเดียวกัน)
- ยังไม่ได้ปิดการขาย

หน้าต่างรอมี 2 แบบ สลับได้ที่แถบด้านบนของ dashboard — **14 วัน** (ค่าเริ่มต้น ดูเดือนล่าสุดได้เร็ว)
และ **30 วัน** (ตัวเลขนิ่งกว่า ไว้ปิดรายงานย้อนหลัง) · แชทที่ยังนับไม่ครบหน้าต่างมีป้าย “ยังรอครบ” กำกับไว้

Instagram และ LINE OA ไม่มีป้ายโฆษณาจาก Meta ช่องนี้จึงแสดงเป็น “—” ในสองช่องทางนั้น
แชทกลุ่มนั้นไปรวมอยู่ใน **ทักทาย/กดเมนู** แทน · **แอดมินไม่ได้ตอบเอง** ไม่นับแชทกลุ่ม
“ทักมาแล้วหาย” อีกต่อไป สองช่องจึงไม่ทับกัน

ทุกไฟล์ใน `data/` และตัวตรวจใน `manifest.json` เข้ารหัส AES-256-GCM ด้วยกุญแจที่ได้จาก
PBKDF2-SHA256 250,000 รอบ จาก `ชื่อผู้ใช้:รหัสผ่าน` การถอดรหัสเกิดในเบราว์เซอร์เท่านั้น
รหัสผ่านไม่ถูกส่งออกไปไหน

## เพิ่มข้อมูลเดือนใหม่

ข้อมูลต้นทาง (`src/`) รหัสผ่าน (`secret.txt`) และ token (`gh_token.txt`) ไม่ได้อยู่ในรีโปนี้ —
เก็บไว้ในเครื่องที่ `Downloads\biopalm_work\hub\` ขั้นตอนคือ

1. วาง `src/fb_YYYY-MM.json` ของเดือนใหม่ และอัปเดต `src/agg.json`
2. `./deploy.sh "ข้อความ commit"` — build ใหม่แล้ว push ขึ้น GitHub Pages

`index.html` จะเปลี่ยนก็ต่อเมื่อแก้โค้ดใน `app.html` เท่านั้น การเพิ่มเดือนไม่ต้องแตะ HTML


## ตัวชี้วัด v4 (ก.ย. 2026)

หลัง `unpack.py` ให้รัน `python3 tools/build_v4.py` ก่อน `build.py` ทุกครั้งที่เพิ่มเดือน — สคริปต์นี้คำนวณกรวย 4 ขั้น
(ทักมาแล้วหาย / คุยแล้ว / ส่งราคาแล้ว / ปิดการขายจากสลิป “ตรวจสอบสลิปสำเร็จ” ที่ชื่อผู้รับ บจก. ไบโอปาล์ม เอ็ดดูเคชั่น),
ยอดโอน, ลีด A/B/C, ความเร็วตอบ (แยกช่องทาง · ช่วงเวลาทำงาน · รายชั่วโมง) และผลงานแอดมิน LINE ลงใน `src/agg.json` คีย์ `v4`
